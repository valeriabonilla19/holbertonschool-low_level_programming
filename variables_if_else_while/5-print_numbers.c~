#include <stdio.h>

/**
* main - Prints all single digit numbers
* Descrption: of base starting from 0
*
* Return: Always 0
*/

int main(void)
{
int a;
for (a = 0; a < 10; a++)

printf("%d", a);

putchar ('\n');

 return (0);
}
