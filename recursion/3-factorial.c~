#include "main.h"

/**
* int factorial - Function that returns the factorial of a given number.
* @n: the function to return
*/

int factorial(int n)
{
if (n < 0)
{
return (-1);
}
if (n == 0)
{
return (1);
}
return (n * factorial(n - 1));
}
